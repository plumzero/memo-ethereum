
在很多开发语言中，会把一些通用、公共的功能封装为库供开发者调用。在 Solidity 语言中，同样为开发者提供了库功能。

在 Solidity 语言中使用`library`关键字来定义库，库创建好后可以作为公共代码被其他合约使用。

库的功能主要是为了提供一些基础功能的函数，通常会声明为 internal 类型。

### 本地导入

导入方式为:
```js
...
import "other.sol";

contract mycontract {
  ...
}
...
```

创建一个[libstring.sol库文件](sol/libstring.sol)并保存。

使用`import`语句导入libstring.sol文件，就可以使用该文件内的库及其功能了。

合约库调用的时候有两种方式，第一种方式是使用"库名.函数名"来调用库函数，第二种方式首先需要使用`using`语句，"using libstring for string"表明string类型可以直接使用libstring，等价于string类型内封装了libstring的全部功能，然后就可以像"a.isEqual(b)"那样调用库函数了。

创建一个[合约文件](sol/test_libstring.sol)并部署测试。

### 从github中导入

开发者可以使用[OpenZeppelin](https://github.com/OpenZeppelin/openzeppelin-contracts)提供的库合约文件进行开发，该库文件包含管理员权限、地址操作、安全数学计算等。

导入方式如下:
```js
...
import "https://github.com/OpenZeppelin/openzeppelin-contracts/blob/master/contracts/token/ERC20/ERC20.sol";

contract Token is ERC20 {
  ...
}
...
```

指定版本号导入:
```js
import "https://github.com/OpenZeppelin/openzeppelin-contracts/blob/v5.5.0/contracts/token/ERC721/ERC721.sol";
```

### 导入 npm 包

去[这里](https://www.npmjs.com/)搜索"openzeppelin"，像下面这样导入:

![](img/contract-import-from-npm.png)

指定版本号导入:
```js
import "@openzeppelin/contracts@5.5.0/token/ERC721/ERC721.sol";
```
